// NVRTC stub for <cuda.h> (CUDA Driver API).
// The driver API is host-only; device code never needs it.
// This stub prevents "cannot open source file" errors when vendor headers
// include <cuda.h> under #ifdef __CUDACC__.
#pragma once
